package com.execel.springboot;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * starts the serverlet for these project
 * 
 * @author Kalaivanan 
 *
 */
@SpringBootApplication
public class Assignment3SpringBootApplication {

	/**
	 * it invoke run method to initialize the servelet
	 * 
	 * @param args
	 */
	public static void main(String[] args) {
		SpringApplication.run(Assignment3SpringBootApplication.class, args);
	}

}
